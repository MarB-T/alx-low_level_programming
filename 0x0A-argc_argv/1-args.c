#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - prints the number of arguments passed
 * @argc: argument count
 * @argv: list of arguments passed
 * Return: 0 success
 */

int main(int argc, char **argv)
{
	printf("%d\n", argc);
	if (argv[argc] == NULL)
	{
	return (0);
	}
	return (0);
}
